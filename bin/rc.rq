#!/bin/sh

PATH=/bin:/usr/bin:/usr/local/bin:/sbin:/usr/sbin:/usr/local/sbin
export PATH

if [ ! -d /rq ] ; then
	# RQ not installed
	exit 0 
fi

me=`id -u`

if [ $me -eq 0 ] ; then
	HOME=/home/br
	export HOME
	exec setuidgid br /etc/init.d/rq $*
fi

case $1 in
	start)
		cd /rq/current
		/rq/current/bin/server_daemon.sh
		/rq/current/bin/queuemgr_ctl start
	;;
	stop)
		cd /rq/current
		/rq/current/bin/queuemgr_ctl stop
		ps ax | grep 'web_server.sh' | grep -v grep | \
		    awk '{print $1}' | xargs kill
	;;
esac

